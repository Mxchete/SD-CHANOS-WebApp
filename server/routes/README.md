# Routes
Directory containing API routing information

Sending requests to ```/api/pot``` will utilize routing functions under the ```pot/``` directory, with functions intended for connecting the pot to the backend

Sending requests to ```/api/user``` will utilize routing functions under the ```user/``` directory, with functions intended for connecting the user client with the backend
